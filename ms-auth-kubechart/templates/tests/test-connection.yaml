apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-auth-kubechart.fullname" . }}-test-connection"
  labels:
    {{- include "ms-auth-kubechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-auth-kubechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
