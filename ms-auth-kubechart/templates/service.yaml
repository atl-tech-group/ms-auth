apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-auth-kubechart.fullname" . }}
  labels:
    {{- include "ms-auth-kubechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-auth-kubechart.selectorLabels" . | nindent 4 }}
